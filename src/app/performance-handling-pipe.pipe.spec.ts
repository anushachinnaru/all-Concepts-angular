import { PerformanceHandlingPipePipe } from './performance-handling-pipe.pipe';

describe('PerformanceHandlingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PerformanceHandlingPipePipe();
    expect(pipe).toBeTruthy();
  });
});
